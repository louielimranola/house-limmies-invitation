# Ambient 3D scene experiment (archived)

A Three.js background: a slow-drifting flyover of the map table with turning
gears and sigil rings, running behind the whole page.

Archived because it read as sliding into a flat 2D image rather than feeling
like a real 3D scene — revisit the camera/depth approach before reviving this.

Files here are a snapshot of index.html, style.css, and ambient-scene.js from
the moment this was set aside, for reference if picked back up later.
